let movie = "Avengers"; // String
let score = 10.89; // Number
let isValid = true; // Boolean
let name; // Undefined
let color = null; //Redefinição


score++; // Incrementa a soma o valor de 1
score--; // Diminui o valor em 1 unidade

console.log(score);

let patientId = 50;
let isEnrolled = true;
const patientInfo = {
  firstName: 'Ana',
  lastName: 'Santos',
};
const patientEmail = 'ana@email.com';

patientId = '50';

// 1.Crie uma costante chamada base e uma variável chamada height e atribua os seus respectivos valores: 5 e 8.

const base = 5;
let height = 8;

// 2.Crie uma constante chamada area e atribua a ela o resultado da multiplicação da base pela altura. Dica: lembre-se de usar sempre o console.log() para imprimir as variáveis e checar os resultados das operações!

let area = base * height;
console.log(area);

// 3.Crie uma constante chamada perimeter e atribua a ela a soma de todos os lados do retângulo.

const perimeter = base + base + height + height;
console.log(perimeter)
